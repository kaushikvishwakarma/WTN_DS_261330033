# Command Line Arguments - Interview Questions

1. **Which module is required to access cmd line arguments?**
   *Answer*: The `sys` module is required. You can access the command line arguments using the `sys.argv` list, where `sys.argv[0]` is the name of the script and the subsequent elements are the arguments passed to it.
